'use strict'
console.log("archivo js en uso")

try {
    var year = 2010;
    alert ( year);
    console.log( decodeURIComponent ( "https://google.com.ar"));

} catch ( error ) {
    alert ("Error: " + error)
}